Nicholas Stewart nss67

run the program simply with python/python3 main.py
this program does not take command line args

follow the terminal instructions to input the inputs
The inputs are in the same order as the sample inputs
after the last domino is entered the program will run

dominos should be entered as follows #: top bottom
be sure to include a space in between the colon and the top string and between the top and bottom
do not repeat numbers either



"In the second stage of search, user iterative deepening starting
from the frontier created in the first stage, until a specified limit"

My specified limits are the following 3 things: no solution, limit reached or goal found
if any of these limits are satisfied the stage 2 search stops hence why my (*) output is shorter than the samples (*) output


